name: Build Branches

on:
  push:
    branches-ignore:
      - main

jobs:
  build_and_test:
    name: "Build and Test"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      
    - uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'maven'
          
    - name: Build with Maven
      run: ./mvnw package -Dquarkus.package.type=uber-jar

    - name: Archive jars
      uses: actions/upload-artifact@v2
      with:
        name: jars
        path: target/international-1.0.0-SNAPSHOT-runner.jar
